/*
import assert from 'node:assert/strict';

class Assert {
    static equal(a: any, ...bs: any[]) {
        bs.map((b) => assert.equal(a, b))
    }
    static notEqual(a: any, ...bs: any[]) {
        bs.map((b: any) => assert.notEqual(a, b))
    }
    static null(...as: any[]) {
        as.map((a) => assert.equal(a, null))
    }
    static notNull(...as: any[]) {
        as.map((a) => assert.notEqual(a, null))
    }
    static hasOwnProperty(a: { hasOwnProperty: (arg0: any) => any; }, ...bs: any[]) {
        bs.map((b) => assert.equal(a.hasOwnProperty(b), true))
    }
    static notHasOwnProperty(a: { hasOwnProperty: (arg0: any) => any; }, ...bs: any[]) {
        bs.map((b) => assert.equal(a.hasOwnProperty(b), false))
    }
}
*/