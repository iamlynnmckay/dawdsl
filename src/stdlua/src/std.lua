return {
    application = require 'std.application',
    array = require 'std.array',
    call = require 'std.call',
    equals = require 'std.equals',
    executor = require 'std.executor',
    export = require 'std.export',
    file = require 'std.file',
    flatten = require 'std.flatten',
    foreach = require 'std.foreach',
    has = require 'std.has',
    indices = require 'std.indices',
    iterator = require 'std.iterator',
    javascript = require 'std.javascript',
    json = require 'std.json',
    log = require 'std.log',
    map = require 'std.map',
    math = require 'std.math',
    modules = require 'std.modules',
    orderedmap = require 'std.orderedmap',
    popen = require 'std.popen',
    size = require 'std.size',
    string = require 'std.string',
    switch = require 'std.switch',
    tostring = require 'std.tostring',
    type = require 'std.type',
    unpack = require 'std.unpack',
    visitor = require 'std.visitor',
}